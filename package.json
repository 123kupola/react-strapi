{
  "name": "strapi-react-starter",
  "version": "1.0.0",
  "description": "Strapi React starter project.",
  "type": "module",
  "scripts": {
    "client": "cd client && npm run dev",
    "server": "cd server && npm run dev",
    "setup:client": "cd client && npm install && node --loader ts-node/esm ../copy-env.mts ./",
    "setup:server": "cd server && npm install && node --loader ts-node/esm ../copy-env.mts ./",
    "setup": "npm install && npm run setup:client && npm run setup:server",
    "dev": "npx concurrently \"cd server && npm run develop\" \"npx wait-on http://localhost:1337 && cd client && npm run dev\"",
    "seed": "cd server && npm run strapi -- import -f ../seed-data.tar.gz --force",
    "export": "cd server && npm run strapi -- export --no-encrypt -f ../seed-data",
    "repo:upstream": "git fetch upstream && git merge upstream/main"
  },
  "dependencies": {
    "@types/node": "^22.5.2",
    "concurrently": "^9.2.1",
    "typescript": "^5.8.3",
    "wait-on": "^9.0.1"
  },
  "devDependencies": {
    "ts-node": "^10.9.2"
  }
}